function startTimer(duration, display)
    let timer = duration;
    let minutes, seconds;

    const intervalId = setInterval(function) {
        minutes = parseInt(timer / 60, 30);
        seconds = parseInt(timer % 60, 30);
        
    }